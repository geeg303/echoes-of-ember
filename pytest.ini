[pytest]
markers =
    integration: exercises multiple production systems together
    scenario: exercises a user-visible lifecycle or campaign journey
    slow: intentionally longer stability or process-level check
testpaths = tests

